Place deployment JSON files here locally, but do not commit them.

Expected local filenames:
- openapi_scm_25c.json
- openapi_scm_26b.json

For production, prefer setting OPENAPI_25C_URL and OPENAPI_26B_URL to remote object storage URLs when files are large.